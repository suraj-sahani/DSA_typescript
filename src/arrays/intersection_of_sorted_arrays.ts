// Given two sorted arrays, return an array containing the intersection of the two arrays
// Repetition is allowed
// Example : 
// arr1 = [1,2,2,3,3,4,5,6], arr2 : [2,3,3,5,6,7]
// res : [2,3,3,4,5]

export function intersection_or_sorted_arrays(arr1: number[], arr2: number[]) {
  const n1 = arr1.length, n2 = arr2.length
  let i = 0, j = 0
  while (i < n1 && j < n2) {
    if (arr1[i]! < arr2[j]!) {

    }
  }
}
